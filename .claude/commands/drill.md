Pick one unchecked problem from `leetcode/todo.md` that matches the pattern in $ARGUMENTS (or any pattern if no args).

Steps:
1. If $ARGUMENTS is specified, filter to that pattern (e.g. "graphs", "dp", "sliding-window"). Otherwise pick a pattern I haven't drilled recently based on `progress.md`.
2. Present the problem: just the LC number, name, and the problem statement. Don't hint at the solution pattern.
3. Ask me what my first instinct is on approach.
4. Enter rubber duck mode. Ask one focused question at a time. Don't write code.
5. When I've coded a working solution, ask me to talk through complexity.
6. After we're done: append the problem to `progress.md` with today's date and add a 3-day redo entry to `leetcode/redo-queue.md`.

If $ARGUMENTS contains a difficulty word (easy/medium/hard), filter by that too. Default to medium.
